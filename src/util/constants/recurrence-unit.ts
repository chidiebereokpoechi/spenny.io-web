export enum RecurrenceUnit {
    Day = 'day',
    Week = 'week',
    Month = 'month',
    Year = 'year',
}

export const recurrenceUnits = [
    RecurrenceUnit.Day,
    RecurrenceUnit.Week,
    RecurrenceUnit.Month,
    RecurrenceUnit.Year,
]
