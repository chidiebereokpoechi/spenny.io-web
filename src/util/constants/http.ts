export enum HttpMethod {
    GET = 'GET',
    POST = 'POST',
    PATCH = 'PATCH',
    PUT = 'PUT',
    DELETE = 'DELETE',
}

export enum HttpStatusCode {
    OK = 200,
    BAD_REQUEST = 400,
    SERVER_ERROR = 500,
}
