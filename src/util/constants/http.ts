export enum HttpMethod {
    Get = 'GET',
    Post = 'POST',
    Patch = 'PATCH',
    Put = 'PUT',
    Delete = 'DELETE',
}

export enum HttpStatusCode {
    Ok = 200,
    BadRequest = 400,
    ServerErrors = 500,
}
